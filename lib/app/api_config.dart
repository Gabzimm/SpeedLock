/// Endereço do backend próprio (o servidor Express na tua VPS,
/// substituindo as Cloud Functions). Trocar pelo domínio real assim que
/// estiver no ar — ver server/README ou a secção de deploy no README
/// principal.
const String apiBaseUrl = 'https://api.speedlock.app';
